import 'package:flutter/material.dart';

class AiScreen extends StatelessWidget {
  const AiScreen({super.key});

  static const id = "ai_screen";  

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}